const { calculateSum } = require("./sum");
const { calculateMultiply } = require("./multiply");
const { calculateDivision } = require("./divide")

module.exports =  {calculateSum, calculateMultiply, calculateDivision};